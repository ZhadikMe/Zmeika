apiVersion: v1
kind: Service
metadata:
  name: snake-game-service
  namespace: app-production
  labels:
    app: snake-game
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 5000
    nodePort: 30000
    protocol: TCP
    name: http
  selector:
    app: snake-game
